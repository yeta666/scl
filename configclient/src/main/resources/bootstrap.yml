server:
  port: 8768
spring:
  application:
    name: SERVICE-CONFIGCLIENT
  cloud:
    config:
      label: master   #分支
      profile: dev    #dev：开发环境、test：测试环境、pro：正式环境
      uri: http://localhost:8767/   #SERVICE-CONFIGSERVER的地址